[project]
name = "aws-cloud-datamanagement-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chin Xu Xin", email = "xchi0019@student.monash.edu" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
aws-cloud-datamanagement-project = "aws_cloud_datamanagement_project:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
